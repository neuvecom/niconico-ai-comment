# プロジェクト概要: ニコニコ動画風 AIコメント生成・描画 Chrome拡張機能

## 1. 目的と背景
ニコニコ動画の視聴画面において、動画の内容（将来的には音声や字幕データ）をもとにAIが生成した複数のキャラクター（ペルソナ）によるコメントを、画面上に弾幕として擬似的に流すChrome拡張機能（Manifest V3）を開発します。
目的は、コメントが少ない動画でも公式の「AIキャラクターコメント」のように賑やかに楽しむこと、およびその画面を録画して「コメント焼き付け動画」として投稿できるようにすることです。
※実際のニコニコのサーバーへは自動投稿せず、ローカル（ブラウザ上）でのみ描画します。

## 2. 技術スタック
- Chrome Extension (Manifest V3)
- 言語: Vanilla JavaScript, HTML, CSS
- 描画方式: ニコニコ動画のプレイヤー要素の上に透過レイヤー（Canvas または CSS Animation）を重ねて描画

## 3. 主要機能（最終ゴール）
1. **動画同期:** ニコニコ動画のプレイヤー（<video>要素）の再生時間（currentTime）を監視・同期。
2. **複数ペルソナ対応:** 「ツッコミ役」「大ファン」「有識者」など、複数の異なるAIキャラのコメントを識別して描画（色やフォントのカスタマイズ対応）。
3. **弾幕エンジン:** コメントが重ならないようにレーンを計算し、右から左へ滑らかに流す。
4. **データ構造:** 以下のようなタイムスタンプ付きのコメントデータ（モック/API経由）を読み込んで再生する。
   [
     { "time": 1.5, "text": "うぽつ！", "user": "fan", "color": "#ffffff" },
     { "time": 3.2, "text": "ここおかしいだろww", "user": "tsukkomi", "color": "#ff0000" }
   ]

## 4. 最初のタスク（Step 1）
まずは、外部API連携などは後回しにし、**「ニコニコ動画のページを開いた際、モックデータ（上記のJSON例）をもとに、実際の動画プレイヤーの上にコメントを滑らかに流す最低限のフロントエンド機能」**を実装したいです。

以下のファイル構成の雛形と、ベースとなるコードを作成してください。
- `manifest.json` (Manifest V3)
- `content.js` (動画要素の検知、再生時間の監視、コメント描画ロジック)
- `content.css` (プレイヤー上に重ねるレイヤーのスタイル)

何から始めるべきか、ディレクトリ構造とコードの提案をお願いします。
