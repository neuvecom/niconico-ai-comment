# プロジェクト設定（テンプレート）

> このファイルはどのプロジェクトでも流用できる CLAUDE.md の雛形です。
> 新しいリポジトリに持ち込んだら `CLAUDE.md` にリネームし、`{{...}}` のプレースホルダを埋め、
> プロジェクトに合わない節（言語別のコードスタイル・環境前提など）を差し替えてください。

{{プロジェクト名}} は {{1〜2行のプロジェクト概要。何を・どの技術で・何のために作るか}} です。
詳細な要件・ロードマップは `{{計画ファイル。例: Plan.md}}` を参照。

## Claude Code 使用時の重要な指示
- **回答言語**: 必ず日本語で回答すること（英語での回答は禁止）
- **出力スタイル**: Explanatory（説明的）モードを使用
  - 実行する操作の理由を明確に説明
  - コードの変更内容を詳細に解説
  - 各ステップの目的と結果を記述

## ツール実行時の許可ルール
- ツール実行（Bash、ファイル操作など）の許可を求めるときは、必ず日本語で説明・確認を行うこと
- 許可を求める際、以下のセキュリティリスクをパーセンテージ(%)で提示すること
  - パスワードや秘密鍵が外に漏れる可能性
  - 外部サーバーにデータが送られる可能性
  - 悪意あるコードが勝手に動く可能性
  - PCの設定が書き換わる可能性

## ドキュメント構成
- `README.md` — 概要・セットアップ・最小限の使い方（外部向け）
- `{{計画/詳細ドキュメント。例: Plan.md / manual.md}}` — {{役割の説明}}
- `docs-dev/work_log/YYYY-MM-DD.md` — Claude Code との作業ログ
- `docs-dev/idea/` — 検討中・保留中のアイデアメモ

## ブランチ運用
- `main` — 安定ブランチ。直接プッシュ禁止、PR ベースでマージ
- `claude/<feature名>` — 機能追加・修正用ブランチ（例: `claude/{{feature例}}`）
  - 1 PR = 1 トピックを原則とする
  - マージ後はブランチ削除して OK

## コミット・PR 規則
- コミットメッセージと GitHub のコメントは日本語で記載
- コミットメッセージ形式：`[種類] 変更内容の説明`
  - 例：`[feat] {{機能例}}を追加`、`[fix] {{不具合例}}を修正`
  - 種類: `feat` / `fix` / `refactor` / `perf` / `docs` / `chore` / `test`
- PR タイトルも同じ形式で日本語

## 作業ログ管理
- Claude Code との作業は `docs-dev/work_log/YYYY-MM-DD.md` に保存
- **重要**: 日付は必ず環境設定の `Today's date` を優先して使用すること
  （ファイル名や既存の日付に惑わされない）
- 日付形式は YYYY-MM-DD
- 1 日に複数トピックある場合は同じファイルに章を分けて追記
- 過去ログの粒度に合わせ、章構成 `実施内容 / 変更 / 検証 / 後方互換性` を意識

## よく使うコマンド
```bash
# 開発フロー（どのプロジェクトでも共通）
git checkout main && git pull origin main && git checkout -b claude/<feature>
git status && git diff
git add <files> && git commit -m "[種類] 説明"
git push -u origin claude/<feature>

# アプリ実行・ビルド・テスト（プロジェクトに合わせて記載）
{{例: npm run dev / python -m src run / pytest など}}
```

## コードスタイル
- 言語: {{使用言語とバージョン。例: Python 3.13+ / TypeScript 5.x / Vanilla JS}}
- インデント: {{2 スペース / 4 スペース / タブ}}
- 文字コード: UTF-8
- 改行コード: LF
- 依存管理: {{requirements.txt / package.json / go.mod など}}
- {{リンタ・フォーマッタ。例: ruff / eslint + prettier}}

## 環境前提
- {{必要なランタイム・SDK。例: Node.js 20+ / Python 3.13+ / Chrome}}
- {{必要な API キー・シークレット。例: `.env` の XXX_API_KEY}}
- {{ローカルで起動が必要な依存サービス。例: DB / ローカルサーバー}}
